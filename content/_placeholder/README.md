# Placeholder

This folder exists only so the `placeholder` Decap CMS collection
(config.yml) has a real path to list — without it, the GitHub API call
for that folder 404s and the collection errors instead of showing an
empty list. It stands in for the 6 boards not yet migrated to
Decap-editable content (see docs/TODO.md). `create: false` in
config.yml, so nothing can be added here through the CMS.
